/**
 * Moved to `@shuv2code/client-runtime/ade/bot-detail` so the mobile bot profile
 * renders bindings, persona versions and the memory save gate from the same
 * rules. Re-exported here because this path is what the web detail panel
 * imports.
 */
export * from "@shuv2code/client-runtime/ade/bot-detail";
