/**
 * Moved to `@shuv2code/client-runtime/ade/needs-you` so the mobile fleet
 * surface decides an item the same way this one does — same filter, same
 * selection, same scope gate, same outcome copy. Re-exported here because this
 * path is what the web inbox and inline renderings import.
 */
export * from "@shuv2code/client-runtime/ade/needs-you";
