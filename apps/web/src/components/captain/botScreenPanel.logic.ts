/**
 * Moved to `@shuv2code/client-runtime/ade/bot-screen`, alongside the phase
 * machine it is presentation for, so a phone that cannot host a viewer can
 * still say exactly which of the seven desktop phases a bot is in.
 * Re-exported here because this path is what the web right rail imports.
 */
export * from "@shuv2code/client-runtime/ade/bot-screen";
