/**
 * Moved to `@shuv2code/client-runtime/ade/bot-model` so the mobile identity
 * sheet offers the same model list, the same capability flag and the same
 * "applies next session" copy. Re-exported here because this path is what the
 * web identity sheet imports.
 */
export * from "@shuv2code/client-runtime/ade/bot-model";
