/**
 * Moved to `@shuv2code/client-runtime/ade/bot-identity` so the mobile bot-mode surface
 * reads the same rules rather than a second copy that drifts. Re-exported here
 * because this path is what the web captain/fleet components import.
 */
export * from "@shuv2code/client-runtime/ade/bot-identity";
